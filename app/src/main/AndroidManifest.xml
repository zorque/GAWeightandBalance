<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.a3dnonsense.ga.gaweightandbalance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustResize">
        <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UseTemplateActivity" />
        <activity android:name=".CreateTemplateActivity" />
        <activity android:name=".OldMainActivity" />
        <activity android:name=".OpenTemplateActivity"></activity>
    </application>

</manifest>